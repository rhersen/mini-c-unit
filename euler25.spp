double euler25(double digits)

zero = one - one
one = digits ÷ digits
two = one + one
three = one + two
nine = three × three
ten = nine + one
limit = one

fib = one
prev = zero
i = one

loop:
i += one
curr = fib
fib += prev
prev = curr
fib < ten ? loop
curr ÷= ten
fib ÷= ten
prev ÷= ten
digits -= one
digits > one ? loop

return i
